# AXI Lite ↔ I2C 신호 웨이브폼 분석 가이드

## 📋 목차
1. [신호 연결 구조](#신호-연결-구조)
2. [확인 순서](#확인-순서)
3. [주요 신호별 확인 포인트](#주요-신호별-확인-포인트)
4. [현재 웨이브폼 분석](#현재-웨이브폼-분석)
5. [문제 진단](#문제-진단)

---

## 🔗 신호 연결 구조

```
[AXI Master (테스트벤치)]
        ↓
[AXI-Lite Interface (i2c_axi_lite_if)]
        ↓ (레지스터)
        ├─ control_reg → i2c_start
        ├─ dev_addr_reg → i2c_ctrl[7:0]
        ├─ mem_addr_reg → i2c_ctrl[15:8]
        ├─ wr_data_reg → wdata
        └─ rd_data_reg ← rdata
        ↓
[I2C Master (i2c_master)]
        ↓ (I2C 프로토콜)
        ├─ scl (Clock)
        └─ sda (Data)
        ↓
[EEPROM 모델 (i2c_eeprom_model)]
```

---

## 🔍 확인 순서

### 1단계: 기본 신호 확인 (0~200ns)
- ✅ `aclk`: 클록이 정상 동작하는지 (주기 10ns)
- ✅ `aresetn`: 리셋이 0→1로 전환되는지 (약 100ns)
- ✅ AXI Write 채널 기본 신호들이 초기화되어 있는지

### 2단계: AXI Write 트랜잭션 확인 (200~600ns)
- ✅ `s_axi_awvalid` / `s_axi_awready`: Address 핸드셰이크
- ✅ `s_axi_wdata`: 레지스터에 쓰는 데이터 확인
  - `0x00000050`: DEV_ADDR 레지스터 (0x04)
  - `0x00000000`: MEM_ADDR 레지스터 (0x08)
  - `0x000000AB`: WR_DATA 레지스터 (0x0C)
  - `0x00000001`: CONTROL 레지스터 (0x00) - START=1
- ✅ `s_axi_bvalid` / `s_axi_bready`: Response 핸드셰이크

### 3단계: I2C 트랜잭션 시작 확인 (600ns~)
- ✅ `i2c_start`: START 펄스 발생 여부
- ✅ `i2c_busy`: BUSY 신호가 1로 설정되는지
- ✅ `state`: I2C 상태 머신이 idle → start로 전환되는지
- ✅ `scl`: I2C 클록이 생성되는지
- ✅ `sda_line`: SDA 신호가 정상적으로 동작하는지

### 4단계: I2C 프로토콜 확인
- ✅ START 조건: SCL=High, SDA High→Low
- ✅ Device Address: 0xA0 (0x50 << 1 | 0)
- ✅ ACK 신호: SDA가 Low로 전환
- ✅ Memory Address: 0x00
- ✅ Data: 0xAB
- ✅ STOP 조건: SCL=High, SDA Low→High

---

## 📊 주요 신호별 확인 포인트

### AXI Write 채널

#### 1. Address 채널
```
신호: s_axi_awaddr, s_axi_awvalid, s_axi_awready
확인:
  - awvalid가 1일 때 awready도 1이어야 함 (핸드셰이크)
  - awaddr 값이 올바른 레지스터 주소인지 확인
    * 0x00000004: DEV_ADDR
    * 0x00000008: MEM_ADDR
    * 0x0000000C: WR_DATA
    * 0x00000000: CONTROL
```

#### 2. Data 채널
```
신호: s_axi_wdata, s_axi_wvalid, s_axi_wready
확인:
  - wvalid가 1일 때 wready도 1이어야 함 (핸드셰이크)
  - wdata 값이 올바른지 확인
    * 0x00000050: Device Address
    * 0x00000000: Memory Address
    * 0x000000AB: Write Data
    * 0x00000001: CONTROL (START=1)
```

#### 3. Response 채널
```
신호: s_axi_bvalid, s_axi_bready, s_axi_bresp
확인:
  - bvalid가 1일 때 bready도 1이어야 함 (핸드셰이크)
  - bresp가 00 (OKAY)인지 확인
```

### AXI Read 채널

#### 1. Address 채널
```
신호: s_axi_araddr, s_axi_arvalid, s_axi_arready
확인:
  - arvalid가 1일 때 arready도 1이어야 함 (핸드셰이크)
  - araddr 값이 올바른 레지스터 주소인지 확인
    * 0x00000014: STATUS
    * 0x00000010: RD_DATA
```

#### 2. Data 채널
```
신호: s_axi_rdata, s_axi_rvalid, s_axi_rready
확인:
  - rvalid가 1일 때 rready도 1이어야 함 (핸드셰이크)
  - rdata 값이 올바른지 확인
    * STATUS: BUSY 비트 확인
    * RD_DATA: 읽은 데이터 (0xAB)
```

### I2C 신호

#### 1. SCL (Serial Clock)
```
신호: scl
확인:
  - 규칙적인 클록 신호 (약 400kHz, 주기 2.5us)
  - START/STOP 조건에서 SCL이 High인지 확인
  - 데이터 전송 중 SCL이 정상 동작하는지 확인
```

#### 2. SDA (Serial Data)
```
신호: sda_line, dut_sda_o, dut_sda_t
확인:
  - START 조건: SCL=High, SDA High→Low
  - Device Address 전송: 0xA0 (MSB 먼저)
  - ACK: SDA가 Low
  - Memory Address 전송: 0x00
  - Data 전송: 0xAB
  - STOP 조건: SCL=High, SDA Low→High

⚠️ 주의: sda_line이 'x' (unknown) 상태이면 문제!
  - dut_sda_t가 1이면 High-Z (정상)
  - dut_sda_t가 0이면 dut_sda_o 값이 sda_line에 반영되어야 함
```

### 내부 신호 (디버깅용)

#### 1. AXI Interface 내부
```
경로: top.dut.u_i2c_axi_lite_if.*
확인:
  - control_reg: CONTROL 레지스터 값
  - dev_addr_reg: Device Address (0x50)
  - mem_addr_reg: Memory Address (0x00)
  - wr_data_reg: Write Data (0xAB)
  - rd_data_reg: Read Data (읽은 데이터)
  - i2c_start: START 펄스
  - i2c_busy: BUSY 신호
  - write_state: Write 상태 머신 (STW_IDLE → STW_WRITE → STW_RESP)
  - read_state: Read 상태 머신 (STR_IDLE → STR_READ → STR_RESP)
```

#### 2. I2C Master 내부
```
경로: top.dut.u_i2c_master.*
확인:
  - state: I2C 상태 머신
    * idle → start → w_d_addr → w_d_addr_a → w_m_addr → ...
  - mem[0]: i2c_ctrl (Control/Address data)
  - mem[1]: wdata (Write data)
  - mem[2]: rdata (Read data)
  - sda_out: SDA 출력 제어
  - counter_data: 비트 카운터 (7 → 0)
  - counter_page: 페이지 카운터
```

#### 3. EEPROM 모델
```
경로: top.eeprom.*
확인:
  - state: EEPROM 상태 머신
  - mem_addr: 메모리 주소
  - bit_cnt: 비트 카운터
  - storage[0]: 메모리 내용 (쓴 데이터가 저장되었는지)
```

---

## 🔎 현재 웨이브폼 분석

### ✅ 정상 동작하는 부분

1. **Clock & Reset**
   - `aclk`: 정상적으로 클록 신호 생성
   - `aresetn`: 100ns 이후 0→1로 전환 (정상)

2. **AXI Write 채널**
   - `s_axi_awready`: 핸드셰이크 정상 동작
   - `s_axi_wdata`: 여러 값으로 변경됨 (0x50, 0xAB, 0x01)
   - `s_axi_bvalid` / `s_axi_bready`: 펄스 발생 (정상)

3. **State 머신**
   - `state`: 0 → 1로 전환 (I2C 트랜잭션 시작)

### ⚠️ 문제가 있는 부분

1. **SDA 신호가 'x' (unknown) 상태**
   - `sda_line = x`: 신호가 정의되지 않음
   - `dut_sda_t = 1`: High-Z 상태 (정상)
   - `dut_sda_o = 0`: 출력이 Low
   - **원인**: SDA 라인이 제대로 구동되지 않거나, EEPROM 모델이 신호를 제대로 받지 못함
   - **해결**: `dut_sda_t`가 0일 때 `dut_sda_o` 값이 `sda_line`에 반영되는지 확인

2. **내부 신호가 'x' 상태**
   - `bit_cnt = xxx`: 비트 카운터가 정의되지 않음
   - `dev_addr = xx`: 디바이스 주소가 정의되지 않음
   - `status_reg = xxxxxxxx`: 상태 레지스터가 정의되지 않음
   - **원인**: 
     - DUT 내부 신호가 웨이브폼에 추가되지 않았거나
     - 초기화되지 않았거나
     - 시뮬레이션이 아직 해당 신호를 업데이트하지 않음
   - **해결**: DUT 내부 신호를 웨이브폼에 추가하거나, 더 긴 시간 범위 확인

3. **AXI Read 채널이 비활성화**
   - `s_axi_arvalid = 0`: Read 요청이 없음
   - `s_axi_rvalid = 0`: Read 데이터가 없음
   - **원인**: 테스트벤치가 아직 Read 트랜잭션을 시작하지 않았거나, 시간 범위가 Write 트랜잭션만 포함
   - **해결**: 더 긴 시간 범위 확인 (약 10us 이상)

---

## 🛠️ 문제 진단

### 문제 1: SDA 라인이 'x' 상태

**증상**: `sda_line = x`, `dut_sda_t = 1`

**원인 분석**:
1. `dut_sda_t = 1`이면 High-Z 상태이므로, `sda_line`은 pull-up 또는 EEPROM 모델에 의해 구동되어야 함
2. EEPROM 모델이 신호를 제대로 구동하지 못하거나
3. SDA 라인 연결에 문제가 있을 수 있음

**확인 방법**:
```verilog
// top.v에서 SDA 라인 연결 확인
assign sda_line = dut_sda_t ? 1'bz : dut_sda_o;

// EEPROM 모델이 sda를 제대로 구동하는지 확인
// tester.v의 i2c_eeprom_model에서:
assign sda = sda_drive ? 1'b0 : 1'bz;
```

**해결책**:
1. `dut_sda_t`가 0일 때 `dut_sda_o` 값이 `sda_line`에 반영되는지 확인
2. EEPROM 모델의 `sda_drive` 신호 확인
3. I2C 트랜잭션이 시작되면 `dut_sda_t`가 0으로 변경되어야 함

### 문제 2: 내부 신호가 'x' 상태

**증상**: `bit_cnt = xxx`, `dev_addr = xx`, `status_reg = xxxxxxxx`

**원인 분석**:
1. DUT 내부 신호가 웨이브폼에 추가되지 않았거나
2. 초기화되지 않았거나
3. 시뮬레이션이 아직 해당 신호를 업데이트하지 않음

**확인 방법**:
1. GTKWave에서 DUT 내부 신호 추가:
   - `top.dut.u_i2c_axi_lite_if.dev_addr_reg`
   - `top.dut.u_i2c_master.state`
   - `top.dut.u_i2c_master.counter_data`
2. 더 긴 시간 범위 확인 (약 10us 이상)
3. Reset 해제 후 내부 신호가 초기화되는지 확인

**해결책**:
1. 웨이브폼에 DUT 내부 신호 추가
2. 시뮬레이션 시간을 늘려서 전체 트랜잭션 확인
3. Reset 후 내부 신호 초기화 확인

### 문제 3: AXI Read 채널이 비활성화

**증상**: `s_axi_arvalid = 0`, `s_axi_rvalid = 0`

**원인 분석**:
1. 테스트벤치가 아직 Read 트랜잭션을 시작하지 않았거나
2. 시간 범위가 Write 트랜잭션만 포함

**확인 방법**:
1. 테스트벤치 코드 확인 (top.v):
   - Write 트랜잭션 완료 후 Read 트랜잭션 시작
   - BUSY 신호 해제 대기 후 Read 트랜잭션 시작
2. 더 긴 시간 범위 확인 (약 10us 이상)

**해결책**:
1. 시간 범위를 늘려서 Read 트랜잭션 확인
2. 테스트벤치에서 Read 트랜잭션이 제대로 시작되는지 확인

---

## 📝 체크리스트

### 기본 확인 사항
- [ ] Clock이 정상 동작하는가? (주기 10ns)
- [ ] Reset이 정상적으로 해제되는가? (약 100ns)
- [ ] AXI Write 핸드셰이크가 정상인가?
- [ ] AXI Read 핸드셰이크가 정상인가?

### AXI Write 확인
- [ ] DEV_ADDR 레지스터에 0x50이 쓰여지는가?
- [ ] MEM_ADDR 레지스터에 0x00이 쓰여지는가?
- [ ] WR_DATA 레지스터에 0xAB가 쓰여지는가?
- [ ] CONTROL 레지스터에 0x01이 쓰여지는가? (START=1)

### I2C 트랜잭션 확인
- [ ] `i2c_start` 펄스가 발생하는가?
- [ ] `i2c_busy` 신호가 1로 설정되는가?
- [ ] I2C 상태 머신이 idle → start로 전환되는가?
- [ ] SCL 신호가 생성되는가?
- [ ] SDA 신호가 정상적으로 동작하는가? (⚠️ 'x' 상태가 아님)
- [ ] START 조건이 발생하는가? (SCL=High, SDA High→Low)
- [ ] Device Address가 전송되는가? (0xA0)
- [ ] ACK 신호가 발생하는가? (SDA Low)
- [ ] Memory Address가 전송되는가? (0x00)
- [ ] Data가 전송되는가? (0xAB)
- [ ] STOP 조건이 발생하는가? (SCL=High, SDA Low→High)

### AXI Read 확인
- [ ] STATUS 레지스터를 읽을 수 있는가?
- [ ] BUSY 신호가 0으로 해제되는가?
- [ ] RD_DATA 레지스터에서 읽은 데이터가 0xAB인가?

### 데이터 일관성 확인
- [ ] Write 데이터(0xAB)와 Read 데이터(0xAB)가 일치하는가?

---

## 🎯 핵심 확인 포인트 요약

1. **AXI 핸드셰이크**: valid/ready 신호가 동시에 1이 되는지 확인
2. **I2C START/STOP 조건**: SCL=High일 때 SDA 변화 확인
3. **I2C Device Address**: 0xA0 (Write) 또는 0xA1 (Read) 확인
4. **I2C Data**: 전송된 데이터가 올바른지 확인
5. **SDA 신호**: 'x' 상태가 아닌 0 또는 1인지 확인 ⚠️
6. **데이터 일관성**: Write 데이터와 Read 데이터가 일치하는지 확인

---

## 💡 팁

1. **시간 범위 조정**: Zoom In/Out으로 특정 구간 확인
2. **마커 사용**: 중요한 시점에 마커를 설정하여 타이밍 확인
3. **신호 그룹화**: AXI 채널별, I2C 신호별로 그룹화하여 확인
4. **내부 신호 추가**: DUT 내부 신호를 추가하여 디버깅
5. **문제 발생 시**: 
   - Reset 해제 시점 확인
   - AXI 핸드셰이크 확인
   - I2C 상태 머신 확인
   - SDA 라인 연결 확인

---

## 📚 참고 자료

- [WAVEFORM_GUIDE.md](./WAVEFORM_GUIDE.md): 웨이브폼 뷰어 사용법
- [WAVEFORM_CHECKLIST.md](./WAVEFORM_CHECKLIST.md): 상세 체크리스트
- [top.v](./verilog/top.v): 테스트벤치 코드
- [i2c_axi_lite_if.v](../../rtl/verilog/i2c_axi_lite_if.v): AXI Interface 코드
- [i2c_master_ver2.v](../../rtl/verilog/i2c_master_ver2.v): I2C Master 코드

